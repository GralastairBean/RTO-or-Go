# TO RUN/STOP 
To run the script on local network run this in terminal...

python -m http.server 8000 --bind 0.0.0.0

To stop just use ctl+c to kill it or end the terminal session

# To see upcoming filaure for de-bugging
Chrome: press f12 -> develeoper tools -> console
If running in vs-code =  spanner symbol top right